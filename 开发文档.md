# 环境法规问答助手 · 开发规格

**版本** v1.0 · **日期** 2026-08-02

## 1. 目标与范围

构建基于 RAG（检索增强生成）的中文环境法规问答系统：用户自然语言提问，系统从本地法规库检索相关条文，由大模型据此生成带出处的回答。

范围限定：
- 数据源：约 20 篇环境法规 PDF（文字版），放于 `data/raw/`
- 全部本地运行，唯一外部依赖为 DeepSeek API
- 界面为 Streamlit 网页应用

## 2. 技术栈（环境已就绪）

| 组件 | 选型 | 版本 |
|---|---|---|
| Python | — | 3.14.5 |
| PDF 解析 | PyMuPDF | 1.27.2.3 |
| 文本切分 | langchain-text-splitters | 1.1.2 |
| 向量模型 | BAAI/bge-small-zh-v1.5 | 本地，CPU |
| 向量库 | chromadb | 1.5.9 |
| LLM | DeepSeek V4 系列 | API |
| LLM 接入 | langchain-openai | 1.4.1 |
| 界面 | streamlit | 1.60.0 |

注意：langchain 主版本为 1.x，代码必须使用 1.x API 写法，不得照抄 0.x 旧教程。

## 3. 目录结构

```
env-law-assistant/
├── data/
│   ├── raw/              # 20 篇法规 PDF（待放入）
│   └── vector_db/        # Chroma 向量库（ingest.py 生成）
├── ingest.py             # 建库脚本
├── app.py                # Streamlit 问答应用
├── requirements.txt      # 依赖清单
├── .env.example          # 环境变量模板
└── 开发文档.md
```

## 4. 系统流程

```
PDF → PyMuPDF 解析 → 切块(500字符,重叠100) → bge 向量化 → Chroma 存储
用户提问 → 向量化 → Chroma 检索 top-k=4 → 拼接条文+问题 → DeepSeek 生成 → 界面展示(含出处)
```

## 5. 模块规格

### 5.1 ingest.py（建库，一次性）

输入：`data/raw/` 下全部 PDF。

处理：
1. 遍历 PDF，逐页提取文本；无文字页跳过并打印警告
2. 记录每段的 `source`（文件名）与 `page`（页码）
3. 用 `RecursiveCharacterTextSplitter` 切块：`chunk_size=500`，`chunk_overlap=100`
4. 用 bge-small-zh-v1.5 向量化
5. 写入 Chroma，collection 名 `env_law`，存储目录 `data/vector_db/`
6. 入库前若 collection 已存在则清空重建（幂等）

输出：
- 向量库 `data/vector_db/`
- 控制台打印：处理的文件数、总块数、平均每文件块数

数据 schema（Chroma records）：
- `id`: 自增唯一
- `document`（正文）: 切块后的文本
- `metadata`: `{"source": 文件名, "page": 页码}`
- `embedding`: 512 维向量（bge-small-zh-v1.5 实测为 512 维）

### 5.2 app.py（问答服务）

界面（Streamlit）：
- 标题与简短说明
- 对话历史保留（`st.session_state`）
- 每轮回答下方展示引用出处（文件名+页码）

流程：
1. 启动时加载 `data/vector_db/` 向量库；不存在则提示先运行 ingest.py
2. 用户问题 → bge 向量化 → Chroma 检索 `n_results=4`
3. 构造提示词（见下）→ DeepSeek 生成 → 展示
4. 错误处理：API 限流/超时给出可读提示

提示词要求：
- 注入检索到的条文（带编号 [1]~[4] 与来源）
- 要求仅基于条文回答；条文不足以回答时明确说明"资料中未找到"
- 回答结尾按编号列出引用来源

## 6. 配置

DeepSeek API：
- `base_url`: `https://api.deepseek.com`
- `model`: DeepSeek V4 系列。默认 `deepseek-v4-flash`，可选 `deepseek-v4-pro`（以平台实际模型 ID 为准，如不一致需按平台文档校正）
- API Key 由用户提供，已就绪

敏感信息：
- Key 存放于 `.env`（`DEEPSEEK_API_KEY=...`），`app.py` 用 `python-dotenv` 读取
- `.env` 不提交；提供 `.env.example` 模板

## 7. 开发步骤与验收

| 步骤 | 内容 | 验收 |
|---|---|---|
| 1 | 数据准备：20 篇 PDF 放入 `data/raw/` | 全部文字版、最新修订版 |
| 2 | 实现 ingest.py | 运行成功，打印统计，生成向量库 |
| 3 | 实现 app.py | `streamlit run app.py` 正常启动 |
| 4 | 联调测试 | 5 个预设问题均能回答且带出处 |
| 5 | 输出 requirements.txt / .env.example | 新环境可复现 |

测试问题示例：
- 建设项目开工前需要办理哪些环保手续？
- 超标排放废气会面临什么处罚？
- 环境噪声污染防治的责任主体是谁？

## 8. 已知约束与风险

- Python 3.14 较新：已验证 8 个关键库可导入，无风险
- bge 模型首次从 HuggingFace 下载，国内网络可能失败：届时配置 `HF_ENDPOINT=https://hf-mirror.com`
- langchain 1.x API 与旧教程不同：开发时以本项目环境为准
- 法规时效性：数据须选用现行有效版本
- DeepSeek 限流：界面需错误提示与重试引导
